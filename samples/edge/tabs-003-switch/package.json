{
  "name": "tabs-003-switch",
  "version": "1.0.0",
  "description": "A simple hello world chrome extension",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cp src/manifest.json dist/ && zip -r dist.zip dist",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.2.1"
  }
}
